# 計画（Plan）: 高画質PixelCatアイコンの生成

## 何を変えるか
- `pixelcats/generate_hd_cats.py` を作成し、TypeScriptデータ(`catPatterns_*.ts`)を読み込んで、すべてのPixelCat個体の高画質画像（420x420ピクセル）を自動生成します。
- 生成された画像は `pixelcats/hd_icons` フォルダ内に保存されます。

## 影響範囲
- 新規フォルダ `pixelcats/hd_icons` と新規スクリプト `pixelcats/generate_hd_cats.py` が追加されます。
- アプリの既存のコードや動作への影響はありません。

##  기대結果
- SNSのアイコンなどにも利用できるレベルの、くっきりとした高画質透過PNGの猫画像が全種類分（数百枚）出力され、一括で利用可能になります。

## 追加対応事項（余白の追加）
- ユーザーからの要望により、書き出される画像の上下左右に余白を追加します。
- スクリプトを修正し、猫本体（420x420）の周囲にそれぞれ `16` ピクセル（指定の「10ピクセルくらい」より少し余裕を持たせた数値）の透明な余白を付与します。（最終解像度は 452x452 となります）
- これにより、SNSでの丸抜きアイコンに設定した際にも、耳などが切れにくくなります。
